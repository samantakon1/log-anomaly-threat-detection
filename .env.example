DB_HOST=localhost
DB_PORT=5432
DB_NAME=log_anomaly_threat_detection
DB_USER=log_anomaly_app
DB_PASSWORD=your_local_password

# DATABASE_URL also works, but DB_PASSWORD is safer when the password contains special URL characters.
# DATABASE_URL=postgresql://log_anomaly_app:your_local_password@localhost:5432/log_anomaly_threat_detection
